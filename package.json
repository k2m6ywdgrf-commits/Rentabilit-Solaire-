{
  "name": "suivi-solaire",
  "private": true,
  "type": "module",
  "description": "Suivi de la rentabilité et de l'amortissement d'une installation solaire (PWA statique + Firebase).",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"node --test test/*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.0",
    "firebase-tools": "^15.25.1"
  }
}
